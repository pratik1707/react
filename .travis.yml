sudo: required
services:
  - docker
  
before_install:
  - docker build -t pratik1707/docker-react -f Dockerfile.dev . 

script:
  - docker run pratik1707/docker-react npm run test -- -- coverage  